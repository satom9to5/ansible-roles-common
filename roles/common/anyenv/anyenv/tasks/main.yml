- name: anyenv directory remove
  ignore_errors: yes
  file: >
    path={{ anyenv_root }}
    state=absent

- name: anyenv download
  git: >
    repo=https://github.com/riywo/anyenv
    dest={{ anyenv_root }}
     
- name: copy anyenv profile.d shell
  template: >
    src=anyenv.sh.j2
    dest=/etc/profile.d/anyenv.sh

- name: anyenv start
  shell: |
    source /etc/profile
