location {{ nginx_kibana_location }} {
    rewrite ^{{ nginx_kibana_location }}/(.+) $1 break;

    proxy_pass     http://{{ nginx_kibana_upstream }}/$1;
    proxy_redirect / $scheme://$http_host{{ nginx_kibana_location }}/;
}
