- name: fluentd elasticsearch output config copy
  tags:
    - fluentd
  template: >
    src=elasticsearch.conf.j2
    dest=/etc/td-agent/output.d/{{ item.tag_name }}.conf
    mode=0755
  with_items: "{{ fluentd_elasticsearch_configs }}"
