fluentd_host: 127.0.0.1
fluentd_port: 24224
fluentd_bind: 0.0.0.0
fluentd_log_dir: /var/log/td-agent
fluentd_buffer_dir: "{{ fluentd_log_dir }}/buffer"
fluentd_buffer_flush_at_shutdown: true
fluentd_buffer_flush_interval: 60
fluentd_buffer_time_slice_wait: 5m
