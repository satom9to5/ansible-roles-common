- name: docker engine install
  tags:
    - docker
  ignore_errors: yes
  yum: >
    name=docker-engine
    state=latest
    enablerepo=dockerrepo

- name: docker engine start
  tags:
    - docker
  service: >
    name=docker
    state=restarted
    enabled=yes
